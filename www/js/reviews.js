class Reviews {
    constructor(id, rating, reviewText, reviewDate, userId, productId) {
        this.id = id
        this.rating = rating
        this.reviewText = reviewText
        this.reviewDate = reviewDate
        this.userId = userId
        this.productId = productId
    }
}